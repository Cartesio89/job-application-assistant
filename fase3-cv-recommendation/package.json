{
  "name": "job-application-assistant",
  "private": true,
  "version": "1.0.0",
  "description": "Dipendenze npm per le Netlify Functions (netlify/functions/*.js). Necessario perche' l'esbuild bundler di Netlify risolve i require() dei pacchetti esterni leggendo node_modules generato da 'npm install' a partire da questo package.json, eseguito automaticamente da Netlify prima della build.",
  "dependencies": {
    "jszip": "^3.10.1",
    "@xmldom/xmldom": "^0.8.10"
  }
}
